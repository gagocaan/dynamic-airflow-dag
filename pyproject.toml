[project]
name = "local-airflow"
version = "0.1.0"
description = "Environment for testing and development purposes."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "apache-airflow>=2.10.3",
    "hydra-core>=1.3.2",
    "loguru>=0.7.2",
]

[dependency-groups]
dev = [
    "ruff>=0.7.3",
]
