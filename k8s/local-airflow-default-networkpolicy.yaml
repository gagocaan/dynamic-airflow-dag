apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: local-airflow-default
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/local-airflow-default: "true"
  podSelector:
    matchLabels:
      io.kompose.network/local-airflow-default: "true"
